
# Golang CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-go/ for more details
version: 2
jobs:
  build:
    # only build the circleci branch for 
    working_directory: ~/gcp/terraform
    steps:
      - run: terraform init
     
