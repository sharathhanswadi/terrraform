



version: 2


description: Lightweight Terraform Orb, not intended for generic use



    
jobs:

  build:
    docker:
      - image: hashicorp/terraform:light      

    steps:        
      - run: cd ~/terrraform &&  terraform init
      - run: cd ~/terrraform && terraform plan
      - run: cd ~/terrraform && terraform apply       
