



version: 2


description: Lightweight Terraform Orb, not intended for generic use



    
jobs:

  build:
    docker:
      - image: hashicorp/terraform:light      

    steps:
      - checkout:
          path:  ~/gcp/terraform            
      - run: terraform init
      - run: terraform plan
      - run: terraform apply   
     
